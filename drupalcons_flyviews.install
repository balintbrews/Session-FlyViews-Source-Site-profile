<?php
/**
 * @file drupalcons_flyviews.install
 * Installation file for the DrupalCon FlyViews installation profile.
 */

/**
 * Implements hook_install().
 */
function drupalcons_flyviews_install() {
  // Use seven as our admin theme.
  theme_enable(array('seven'));
  $variables = array(
    'admin_theme' => 'seven',
    'node_admin_theme' => 1,
  );
  foreach ($variables as $key => $variable) {
    variable_set($key, $variable);
  }
}

